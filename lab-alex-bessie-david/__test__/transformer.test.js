'use strict';

const transformer = require('../lib/transformer.js');
require('jest');

describe('Transformer Module', function() {
  describe('#blackout', () => {
    it('not sure', (done) => {
      // write testing
      
      done();
    });
  });
  describe('#invert', () => {
    it('something', (done) => {
      // write testing

      done();
    });
  });
});