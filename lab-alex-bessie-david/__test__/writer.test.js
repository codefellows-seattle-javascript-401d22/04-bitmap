'use strict';

const writer = require('../lib/writer.js');
require('jest');

describe('Writer Module', function() {
  describe('#writer', () => {
    it('something', done => {
      // write tests here
      
      done();
    });
  });
});